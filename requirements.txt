# Core
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Embeddings (local, no API needed at rank time)
sentence-transformers>=3.0.0
torch>=2.2.0

# Vector math
numpy>=1.26.0
scikit-learn>=1.5.0

# Data processing
orjson>=3.10.0
pandas>=2.2.0

# LLM APIs (pre-computation phase only, not used during ranking)
anthropic>=0.40.0
openai>=1.50.0

# Utilities
tiktoken>=0.7.0
python-dotenv>=1.0.0
httpx>=0.27.0
pyyaml>=6.0.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0

# Linting
ruff>=0.7.0
